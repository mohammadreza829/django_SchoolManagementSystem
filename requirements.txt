# ==============================================================
#  وابستگی‌های اجرای پروژه
#  نصب:  pip install -r requirements.txt
# ==============================================================

# فریم‌ورک اصلی
Django>=6.0,<6.1

# درایور PostgreSQL
# نسخهٔ binary را می‌گیریم تا روی ویندوز نیازی به کامپایلر C نباشد.
# Django 6.0 هم psycopg2 (۲٫۹٫۹+) و هم psycopg 3 را پشتیبانی می‌کند؛ فعلاً
# روی نسخهٔ ۲ مانده‌ایم که در محیط توسعه نصب است.
# مهاجرت به psycopg 3 بعداً توصیه می‌شود (پشتیبانی psycopg2 روزی حذف خواهد شد).
psycopg2-binary>=2.9.9

# لازم برای ImageField (تصویر دوره، آواتار، تصویر سوال)
Pillow>=11.0

# توجه: برای فراخوانی API هوش مصنوعی وابستگی اضافه‌ای لازم نیست؛
# qa/services.py عمداً از urllib کتابخانهٔ استاندارد پایتون استفاده می‌کند.

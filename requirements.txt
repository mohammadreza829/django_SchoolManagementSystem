# ==============================================================
#  وابستگی‌های اجرای پروژه
#  نصب:  pip install -r requirements.txt
# ==============================================================

# فریم‌ورک اصلی
Django>=6.0,<6.1

# درایور PostgreSQL (نسخهٔ binary تا نیازی به کامپایل نداشته باشی)
psycopg[binary]>=3.2

# لازم برای ImageField (تصویر دوره، آواتار، تصویر سوال)
Pillow>=11.0

# برای فراخوانی API هوش مصنوعی (EduPlatform/ai_settings.py)
requests>=2.32

# سرور WSGI برای اجرای واقعی (runserver فقط برای توسعه است)
gunicorn>=23.0

# سرو فایل‌های استاتیک در محیط واقعی بدون nginx
whitenoise>=6.7
